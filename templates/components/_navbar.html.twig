{# templates/components/_navbar.html.twig #}
<nav class="navbar navbar-expand-lg navbar-dark bg-card border-bottom border-mindforge sticky-top">
    <div class="container-fluid px-4">
        <a class="navbar-brand d-flex align-items-center" href="{{ path('app_home') }}">
            <i class="bi bi-hexagon-fill text-primary-mf fs-4 me-2"></i>
            <span class="montserrat fw-bold fs-4">
                Mind<span class="text-primary-mf">Forge</span>
            </span>
        </a>
        
        <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto align-items-center">
                {% if app.user %}
                    <li class="nav-item me-3">
                        <span class="badge bg-card border border-mindforge text-accent">
                            <i class="bi bi-lightning-charge-fill me-1"></i>
                            {{ app.user.gamificationStats.xpTotal|default(0) }} XP
                        </span>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle text-light d-flex align-items-center" href="#" data-bs-toggle="dropdown">
                            <div class="rounded-circle bg-primary-mf d-flex align-items-center justify-content-center me-2" 
                                 style="width: 35px; height: 35px; font-size: 0.85rem;">
                                <span class="fw-bold">{{ app.user.firstName|first|upper }}{{ app.user.lastName|first|upper }}</span>
                            </div>
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end bg-card border-mindforge">
                            <li><a class="dropdown-item text-light" href="{{ path('app_profile') }}">
                                <i class="bi bi-person me-2"></i>Profil
                            </a></li>
                            <li><hr class="dropdown-divider border-mindforge"></li>
                            <li><a class="dropdown-item text-danger" href="{{ path('app_logout') }}">
                                <i class="bi bi-box-arrow-right me-2"></i>Déconnexion
                            </a></li>
                        </ul>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>