{% extends 'admin/base.html.twig' %}

{% block title %}Gestion des Salles Virtuelles{% endblock %}

{% block content %}
<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-md-8">
            <h1>Gestion des Salles Virtuelles</h1>
        </div>
    </div>

    {% if rooms is empty %}
        <div class="alert alert-info">
            Aucune salle virtuelle disponible.
        </div>
    {% else %}
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>Nom</th>
                        <th>Sujet</th>
                        <th>Créateur</th>
                        <th>Participants</th>
                        <th>Capacité Maximale</th>
                        <th>Statut</th>
                        <th>Créée le</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for room in rooms %}
                        <tr>
                            <td>{{ room.name }}</td>
                            <td>{{ room.subject }}</td>
                            <td>{{ room.creator.firstName }} {{ room.creator.lastName }}</td>
                            <td>{{ room.participants|length }}</td>
                            <td>{{ room.maxParticipants }}</td>
                            <td>
                                {% if room.isActive %}
                                    <span class="badge bg-success">Actif</span>
                                {% else %}
                                    <span class="badge bg-danger">Fermé</span>
                                {% endif %}
                            </td>
                            <td>{{ room.createdAt|date('d/m/Y H:i') }}</td>
                            <td>
                                <form method="POST" action="{{ path('admin_room_toggle', {id: room.id}) }}" style="display:inline;">
                                    <input type="hidden" name="_token" value="{{ csrf_token('toggle_room_' ~ room.id) }}">
                                    <button type="submit" class="btn btn-sm {% if room.isActive %}btn-warning{% else %}btn-info{% endif %}">
                                        <i class="fas fa-toggle-{{ room.isActive ? 'on' : 'off' }}"></i>
                                        {{ room.isActive ? 'Fermer' : 'Ouvrir' }}
                                    </button>
                                </form>
                                <form method="POST" action="{{ path('admin_room_delete', {id: room.id}) }}" style="display:inline;" onsubmit="return confirm('Êtes-vous sûr ?');">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete_room_' ~ room.id) }}">
                                    <button type="submit" class="btn btn-sm btn-danger">
                                        <i class="fas fa-trash"></i> Supprimer
                                    </button>
                                </form>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    {% endif %}
</div>
{% endblock %}
